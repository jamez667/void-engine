# Rust
/target
# Alternate target dirs, used to build while a game holds the exe in /target.
/target-*

# Claude tooling state
.claude/settings.local.json
.claude/worktrees/

# Misc
*.original.md
nul

# Preview renders are artefacts, not sources.
materials.png
*.png
